#include <iostream>
#include <sstream>
#include <string>

using std::cin;
using std::cout;
using std::endl;

int main(){
    std::string input;
    
    do {
        std::getline(cin, input);
        // process command 
 
    } while (input != "X");
    return 0;
    
    
}
